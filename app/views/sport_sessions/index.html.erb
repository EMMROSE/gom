<div id="map"
  data-query="<%= params[:query].to_json %>"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  style="height: calc(100vh - 122px); width: 100%;position: relative;">
  <button data-toggle="modal" data-target="#sport-session-creation-modal" class="btn-add-sesh-map">Create a new Session</button>
</div>
